# Correction — Mini TP (2h) flipper (toutes equipes)

Fichier reserve a la correction enseignant du mini TP:
- source etapes 1 a 5
- interpretation
- correction du challenge final

## Structure suggeree

1. Chargement et controle (`games`, joueurs uniques)
2. Nettoyage minimal (`game_duration_s`, `tilt_risk_pct`, `pause_s`)
3. KPI par zone arcade (`arcade_zone`)
4. Barplot du taux d'erreur
5. Boxplot `game_duration_s` par `machine_type`
6. Challenge `risk_score` par `machine_id`
